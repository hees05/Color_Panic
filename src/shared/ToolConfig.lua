local ToolConfig = {
    DROP_COUNT = 5, --number of items to be dropped on the round, I need to change this based on the users left on the game arena. 
    LIFETIME_SEC = 25, --despawning if the item wasn't picked up by the user
    NAMES = {} --names of all the tools available
}